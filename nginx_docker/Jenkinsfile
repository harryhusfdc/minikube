pipeline{
    agent any
    stages{
        stage('Create Image'){
            steps{
                echo "Creating image"
                dir("${env.WORKSPACE}"){
                    sh """
                        pwd
                        docker build -f nginx_docker/Dockersfile -t harryhu/simple-nginx:0.0.1 nginx_docker
                        docker push harryhu/simple-nginx:0.0.1
                    """
                }
            }
        }
    }
}